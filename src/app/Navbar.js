const Navbar = () => {
  return (
    <nav>
      <div>
        <h2>Would you rather...?</h2>
      </div>
      <div>
        <p>Welcome, user</p>
        <button type="button">Logout</button>
      </div>
    </nav>
  );
};

export default Navbar;
